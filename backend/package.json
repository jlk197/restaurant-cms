{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage --detectOpenHandles",
    "test:watch": "jest --watch",
    "dev": "nodemon index.js",
    "migrate": "cross-env DATABASE_URL=postgres://cms_user:cms_password@localhost:5432/cms_db node-pg-migrate",
    "migrate:up": "cross-env DATABASE_URL=postgres://cms_user:cms_password@localhost:5432/cms_db node-pg-migrate up",
    "migrate:down": "cross-env DATABASE_URL=postgres://cms_user:cms_password@localhost:5432/cms_db node-pg-migrate down",
    "migrate:create": "node-pg-migrate create"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/migrations/"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "node-pg-migrate": "^8.0.3",
    "nodemailer": "^7.0.12",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
